// Dadas as variáveis:
// ```jsx
// let p = true;
// let q = false;
// ```
// Determine e exiba o resultado das seguintes expressões:
// - `p && q`
// - `p || q`
// - `!p`
// - `!q`

let p = true;
let q = false;

console.log(p && q);
console.log(p || q);
console.log(!p);
console.log(!q);
